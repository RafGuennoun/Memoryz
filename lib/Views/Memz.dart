import 'package:flutter/material.dart';

class Memz extends StatelessWidget {
  const Memz({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Memz"),
    );
  }
}